if [ $# -eq 0 ]
  then
    echo "Give a commit msg"
    exit
fi

python3 generate.py deploy
git checkout master
find ./* -prune -not -name "dist" | xargs rm -rf
mv dist/* ./
rm -rf dist
git add .
git commit -am "$1"
git push
git checkout gen